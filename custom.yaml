# ------------------------------------------------------------------
# External dependencies not in upstream rosdep
# ------------------------------------------------------------------
plantuml:
  ubuntu:
    '*': [plantuml]
python3-viser-pip:
  ubuntu:
    pip:
      packages: [viser]
