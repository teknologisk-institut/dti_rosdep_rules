# ------------------------------------------------------------------
# External dependencies not in upstream rosdep
# ------------------------------------------------------------------
plantuml:
  ubuntu:
    '*': [plantuml]
python3-viser-pip:
  ubuntu:
    pip:
      packages: [viser]
python3-small_gicp-pip:
  ubuntu:
    pip:
      packages: [small_gicp]
